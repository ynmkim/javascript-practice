let dataType = ['number', 'string', 'boolean', 'null', 'undefined', 'object'];

// 여기에 코드를 작성하세요
let arrayLength = dataType.length;

for(let i = 0; i < arrayLength; i++){
  console.log(dataType[i])
}

